# Example file for sensitive values.  Copy to `config.d/secrets.yaml`
# and fill in your credentials.  Never commit the populated file.

tinkoff:
  token: ${TINKOFF_TOKEN}
  account_id: ${TINKOFF_ACCOUNT_ID}
  sandbox: true

telegram:
  token: ${TELEGRAM_TOKEN}
  chat_id: ${TELEGRAM_CHAT_ID}
  allowed_users: []
